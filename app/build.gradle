plugins {
    id "com.android.application"
}

android {
    namespace "com.wintopic.codexpanel"
    compileSdk 35

    defaultConfig {
        applicationId "com.wintopic.codexpanel"
        minSdk 23
        targetSdk 35
        versionCode 2
        versionName "1.1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            signingConfig signingConfigs.debug
        }
    }
}
